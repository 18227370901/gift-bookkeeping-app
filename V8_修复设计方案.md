---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '031dc0a9-b3af-45a8-b789-5f15ab45b79e'
  PropagateID: '031dc0a9-b3af-45a8-b789-5f15ab45b79e'
  ReservedCode1: '3b2a0d15-1733-4f08-96a3-c854781377ca'
  ReservedCode2: '3b2a0d15-1733-4f08-96a3-c854781377ca'
---

# 人情记账宝 V8 修复设计方案（已确认，开始实施）

> 分支：`feature/ai_assistant`（禁止改 main）｜日期：2026-09-11
> 状态：**用户已确认 5 点调整意见，方案定稿，开始改代码**

---

## 一、需求总览

用户提出 12 项需求，分为三组：

| 编号 | 模块 | 类型 | 需求 |
|------|------|------|------|
| 1 | 工单页面 | 新增 | 列表排序功能 |
| 2 | 工单页面 | 新增 | 筛选功能扩展（已有状态筛选，需评估是否补充） |
| 3 | 工单页面 | 新增 | 仅管理员可删除工单 |
| 4 | 工单页面 | 新增 | 分页，默认 10 条/页，用户可自定义每页条数 |
| 5 | 工单页面 | 优化 | 页面排版布局优化 |
| 6 | webhook 页面 | 修复 | 测试按钮无反应 |
| 7 | webhook 页面 | 优化 | 测试过程（loading）与结果（成功/失败）明确提示 |
| 8 | webdav 页面 | 修复 | 加密密码回显改为"保留已输入密码"逻辑 |
| 9 | webdav 页面 | 修复 | 普通用户不可编辑/删除管理员定时任务 → 按钮置灰 |
| 10 | webdav 页面 | 修复 | 手动备份只备份当前用户有权限菜单的数据 |
| 11 | webdav 页面 | 新增 | "一键引用管理员配置"按钮（去敏） |
| 12 | webdav 页面 | 权限 | 本地数据库备份与恢复仅管理员可见可用（**已调整为：普通用户可备份/恢复自己的数据和配置，仅系统全局配置不可备份**） |

> **用户确认调整（2026-09-11）**：
> 1. 备份范围方案 B 确认
> 2. 一键引用改为：**一键复制 + 一键更新**（避免管理员更新配置后其他用户无法获取最新数据）
> 3. 本地备份卡片**不隐藏**：普通用户可备份/恢复自己创建的数据和配置，仅系统全局配置（如 AI 配置、管理员 WebDAV 配置、定时任务等）不可备份
> 4. 加密密码回显规则：**仅当未勾选「勾选此项清除已保存的加密密码」时才回显**；勾选则置空
> 5. webhook 测试无反应：**优先检查测试响应时长过长导致**，优化超时并给出提示语，避免页面看起来像无反应

---

## 二、现状探查结论（关键事实）

### 2.1 工单页面（`permission_tickets_view` 路由 + `permission_tickets.html`）
- 后端路由 `routes_ext.py` L4182-4204：仅支持 `?status=` 筛选，`query.all()` 一次性加载全部，**无排序参数、无分页、无删除路由**
- 列表固定按 `created_at.desc()` 排序
- 模板：筛选仅"全部/待审批/已批准/已驳回"四个链接，**缺"已撤销"筛选**；无分页控件；无删除按钮
- 已有操作：批准 / 驳回 / 撤销（均为管理员）

### 2.2 webhook 测试（`admin_test_webhook` + `admin_webhooks.html`）
- 后端路由已存在（`POST /admin/webhook/test/<id>`，L3151-3172），返回 JSON
- 前端 JS（L1004-1037）：按钮绑定 `.btn-test-webhook`，已有 loading 转圈 + `alert()` 提示
- **用户反馈"测试按钮无反应"**：经排查 `webhook_utils.py` L410 `test_single_webhook` 调用 `_send_payload(url, payload, headers, timeout=12)`，**默认超时 12 秒**；前端 JS 无超时处理，若目标 URL 响应慢/网络不通，按钮会一直停在"测试中..."状态，看起来像"无反应"。**确认为超时过长问题，优化方案见下文需求 6/7**

### 2.3 webdav 页面（`admin_backups.html` + 相关路由）
- **加密密码**（L62-64）：V7 改为不回显明文（placeholder="已设置，留空保持不变"）。用户要求改回"**保存后密码框保留已输入密码**"，仅勾选"清除此项加密密码"才清空
- **定时任务**（L223-263）：操作列有历史/启停/编辑/删除按钮，**未按任务创建者置灰**；后端已校验（非本人返回 403/flash）
- **备份范围**（`build_user_scoped_backup_db` L41-81）：目前仅过滤 `gift_records`、`banquets`、`anniversary_reminders` 三张表，**其他全局表（users、backup_configs、scheduled_backup_tasks、webhook_configs、ai 配置、system_settings、permission_tickets、webhook_logs、operation_logs 等）全部包含在备份中**
- **一键引用管理员配置**：普通用户配置表单（L97-136）**无该按钮**
- **本地备份**（L355-384）：卡片**对所有用户可见**，仅后端接口做了权限校验

### 2.4 数据表清单（models.py，共 22 张表）
| 表名 | 归属 | 是否应纳入普通用户备份 |
|------|------|----------------------|
| gift_records / banquets / anniversary_reminders | 用户数据 | ✅ 已过滤 |
| users | 全局 | ❌ 不应 |
| chat_sessions / chat_messages / ai_query_logs | AI 模块 | ❌ 不应（无权限时） |
| backup_configs | 全局+用户私有 | ❌ 不应（含管理员 WebDAV 凭证） |
| scheduled_backup_tasks / scheduled_task_execution_logs | 全局/用户 | ❌ 不应 |
| webhook_configs / webhook_logs | 全局 | ❌ 不应 |
| operation_logs / security_risks / login_risks | 全局 | ❌ 不应 |
| system_settings / registration_tokens | 全局 | ❌ 不应 |
| broadcasts / broadcast_reads / shared_ledger_links | 全局 | ❌ 不应 |
| backup_attachments | 全局 | ❌ 不应 |
| permission_tickets | 全局 | ❌ 不应（含他人工单） |
| users | 全局 | ❌ 不应 |

---

## 三、设计方案（逐项）

### 需求 1+2+4：工单列表排序、筛选扩展、分页

**后端**（`routes_ext.py` `permission_tickets_view`）：
- 新增 URL 参数：`sort`（排序字段，可选 `created_at` / `updated_at` / `status`）、`order`（`asc` / `desc`，默认 desc）、`page`（页码，默认 1）、`per_page`（每页条数，默认 10，允许 5/10/20/50/100）
- 管理员：`status` 筛选保留 + 增加 `revoked`；普通用户同样支持分页（按自己的工单）
- 使用 `query.paginate()` 或 `limit/offset` 实现分页
- 传递 `pagination` 对象 + 当前 `sort/order/per_page` 回模板

**前端**（`permission_tickets.html`）：
- 表格列头加"提交时间"可点击排序（升降序切换箭头）
- 筛选栏补上"已撤销"状态按钮
- 表格下方新增分页条（上一页/页码/下一页 + 每页条数下拉选择器）
- 保留 URL 参数（排序/筛选/页码联动，翻页后不丢失）

### 需求 3：仅管理员删除工单

**后端**（`routes_ext.py`）：
- 新增 `POST /permission_tickets/<id>/delete`，**仅管理员**可调用
- 删除工单前检查存在性；删除后 `safe_log` 记录 + 尝试 webhook 推送
- 普通用户访问返回 403/flash

**前端**：
- 管理员操作列（非 pending 状态）增加"删除"按钮（带 `confirm()` 二次确认）
- 普通用户看不到删除按钮

### 需求 4：分页（并入需求 1+2 实现）

### 需求 5：工单页排版优化

- 列表卡片顶部增加"共 N 条 / 当前页"统计信息
- 状态徽章 + 操作按钮间距统一、对齐优化
- 小屏响应式调整（操作列允许换行）

### 需求 6+7：webhook 测试按钮修复 + 过程/结果提示（已定位根因：超时过长）

**根因（已确认）**：`webhook_utils.py` L410 `test_single_webhook` → `_send_payload(url, payload, headers, timeout=12)`，**后端超时 12 秒**；前端 JS 无超时控制，且请求期间按钮停在"测试中..."状态。当目标 URL 响应慢、网络不通或被墙时，12 秒内页面无任何反馈，**看起来像"点击无反应"**。

**修复方案**：
1. **后端缩短测试超时**：`webhook_utils.py` `test_single_webhook` 的 `_send_payload` 调用超时从 12 秒降到 **5 秒**（`timeout=5`），保证测试响应可控
2. **前端增加超时保护**：fetch 请求加 `AbortController` 10 秒超时，超时即中断并提示"测试超时，请检查目标地址或网络"
3. **交互增强**：
   - 点击后按钮立即 loading（禁用 + 转圈 + "测试中..."）
   - 结果用 **toast 替换原生 `alert()`**：成功绿色 toast（含状态码），失败红色 toast（含错误信息与状态码）
   - 若响应在 2 秒内未返回，追加提示"正在等待目标服务器响应，请稍候..."（防"无反应"感知）

### 需求 8：加密密码回显逻辑变更

**当前逻辑**（V7 已改）：`admin_save_webdav_config` clear 优先，密码框不回显明文，placeholder 提示"已设置，留空保持不变"。

**用户要求（已确认）**：改为"**原来的逻辑**"——即：
- 用户输入密码保存后，页面刷新时密码框**保留刚输入的密码**（回显）
- **仅当勾选「勾选此项清除已保存的加密密码」时，输入框才置空**

**实现方案**：
- `admin_backups()` 路由：将 `config.backup_encrypt_password`（明文）传入模板变量（如 `config_encrypt_pwd_plain`）
- 模板密码框 `value="{{ config_encrypt_pwd_plain or '' }}"`（回显明文）
- **勾选"清除"复选框时 JS 清空输入框并禁用**（现有 `toggleClearEncryptPwd()` 逻辑保留）；**未勾选时正常回显**
- **注意安全权衡**：回显明文仅限**当前用户自己的配置**，管理员配置不回显给普通用户（普通用户没有该输入框，天然满足）
- 说明：加密密码仅在 WebDAV 加密备份/恢复时使用，非登录密码，回显属可控风险；此为用户明确要求（与 V7 的去明文策略相反，以最新要求为准）

### 需求 9：定时任务按钮置灰

**现状**：后端已隔离（普通用户编辑/删除/启停他人任务被拒），但前端按钮可点。

**方案**（`admin_backups.html` 定时任务表格）：
- 模板中计算 `can_operate = current_user.is_admin or t.created_by == current_user.id`
- 对管理员创建且当前用户非管理员的：
  - 编辑/删除/启停按钮加 `disabled` 属性
  - 追加 `title="仅创建者或管理员可操作"` 提示
  - 执行历史按钮保留可点（查看不影响数据）
- 后端不变（已有权限兜底）

### 需求 10：备份范围限定（关键安全项）—— 方案 B（用户已确认）

**现状**：`build_user_scoped_backup_db` 仅过滤 3 张用户表，其余全局表全量进入备份文件，普通用户下载后可看到管理员 WebDAV 配置、其他用户信息等。

**方案 B（用户已确认）**：
- 在临时库中，先 `DELETE` 用户数据表非本人数据（gift_records/banquets/anniversary_reminders 按 user_id），再对**非用户数据全局表**执行 `DROP TABLE`，彻底移除：
  - `users`、`backup_configs`、`scheduled_backup_tasks`、`scheduled_task_execution_logs`、`webhook_configs`、`webhook_logs`、`operation_logs`、`login_risks`、`security_risks`、`system_settings`、`registration_tokens`、`broadcasts`、`broadcast_reads`、`shared_ledger_links`、`backup_attachments`、`permission_tickets`、`chat_sessions`、`chat_messages`、`ai_query_logs`
- 保留：`gift_records`、`banquets`、`anniversary_reminders`（按 user_id 过滤后的本人数据）
- **恢复侧**：`admin_upload_local_backup` / `admin_restore_webdav_backup` 恢复后已调用 `init_database()`（L3664/L3818）自动补建缺失表，无需额外改动，方案 B 闭环成立

### 需求 11：一键引用管理员配置（去敏 + 一键更新）—— 用户已确认

**方案**：
- 新增后端接口：`GET /admin/backups/reference_admin_config`（**仅普通用户可调用**，管理员无此按钮）
- 返回管理员全局配置的**去敏字段**：
  - `webdav_url`（服务器地址）✅
  - `webdav_username`（账号）✅
  - `backup_subdir` / `backup_path`（子目录）✅
  - **密码字段不返回**（`webdav_password` 置空 / 返回 `has_password: true` 提示"密码请自行填写"）
- 普通用户配置表单（L97-136）增加两个按钮：
  - **「一键引用管理员配置」**：点击 → JS fetch 获取 → 自动填充服务器地址/账号/子目录三个输入框 → 提示"已引用，密码请自行填写"
  - **「一键更新」**：点击同样调用接口重新拉取管理员最新配置并覆盖填入（**解决管理员更新配置后用户无法获取最新数据的问题**）
  - 若管理员未配置：提示"管理员尚未配置 WebDAV"
- 权限：仅普通用户（非管理员）显示；服务端校验当前用户非管理员才可调用

### 需求 12：本地备份与附件功能（用户已确认：不隐藏，普通用户可备份/恢复自己的数据）

**用户确认**：不需要隐藏。普通用户**不能备份系统全局配置**，但**可以备份和恢复用户自己创建的数据和配置**。

**方案**：
- **前端不隐藏卡片**：本地数据库备份/附件恢复卡片保持所有有备份权限的用户可见
- **后端调整**：普通用户执行"下载本地备份"时，**同样走 `build_user_scoped_backup_db` 过滤**（`admin_download_local_backup` 目前直接下载完整库，需改为：普通用户下载临时过滤库）；上传恢复 .db 时恢复的是**过滤后的库**，恢复流程走 `init_database()` 补表，天然支持
- 即：备份恢复对普通用户开放，但**备份文件内容仅含本人数据，不含系统全局配置**（需求 10 方案 B 已覆盖）

---

## 四、涉及文件清单

| 文件 | 改动内容 |
|------|----------|
| `routes_ext.py` | 工单排序/分页/删除路由；`build_user_scoped_backup_db` 表过滤（删全局表）；本地下载备份走过滤库；`/admin/backups/reference_admin_config` 去敏接口 |
| `webhook_utils.py` | `test_single_webhook` 超时 12s→5s |
| `templates/permission_tickets.html` | 排序/筛选（含已撤销）/分页/删除按钮/排版优化 |
| `templates/admin_webhooks.html` | 测试交互增强（toast 替代 alert、前端超时、loading 体验） |
| `templates/admin_backups.html` | 加密密码回显（未勾选清除时）；定时任务按钮置灰；一键引用+一键更新按钮；卡片不隐藏 |
| `models.py` | 无改动（恢复自动建表已有） |

---

## 五、验证计划

1. 后端语法检查 `python -m py_compile`
2. 重启服务（`.temp\start_app.bat`），端口 11443
3. 浏览器逐项验证：
   - 管理员登录 → 工单页：排序/筛选（含已撤销）/分页（默认 10 条/自定义）/删除工单
   - 普通用户登录 → 工单页：分页 + 无删除按钮
   - webhook 测试按钮：点击后 loading → 结果 toast（成功/失败）→ 超时提示
   - webdav 页（管理员）：加密密码保存后刷新保留；本地备份卡片可见
   - webdav 页（普通用户）：定时任务管理员按钮置灰；一键引用+一键更新（不含密码）；本地备份卡片可见但备份内容仅本人数据
   - 普通用户执行备份 → 下载备份文件 → 检查 SQLite 中是否含 users/backup_configs 等敏感表
4. 更新 `AI_ASSISTANT_DESIGN.md`（追加 V8 章节）+ `README.md`
5. Git 提交（feature/ai_assistant）

---

## 六、风险与已确认结论

| # | 问题 | 结论 |
|---|------|------|
| 1 | 加密密码回显与 V7 去明文策略冲突 | **已确认**：未勾选清除时回显，勾选清除则置空 |
| 2 | 备份范围方案 B（删全局表+恢复自动建表） | **已确认**采用方案 B |
| 3 | 一键引用实现方式 | **已确认**：一键复制 + 一键更新（非实时同步） |
| 4 | 本地备份卡片是否隐藏 | **已确认**：不隐藏，普通用户可备份/恢复自己数据，系统配置不入库 |
| 5 | webhook 测试无反应 | **已定位**：后端超时 12s 过长 + 前端无超时提示，优化为 5s 后端超时 + 前端 10s 超时 + toast 提示 |

---

## 七、下一步

**方案已定稿并获用户确认**，下一步：按方案实施代码修改 → 语法检查 → 重启服务 → 浏览器逐项验证 → 更新设计文档 → Git 提交。

> AI生成